version: 1
piri:
  nodes:
    - storage:
        db: sqlite # or 'postgres'
        blob: filesystem # or 's3'
    # supports a total of 9 piri nodes, 9 due to anvil wallet limit
    #- storage:
    #    db: sqlite
    #    blob: filesystem
    #- storage:
    #    db: postgres
    #    blob: s3
