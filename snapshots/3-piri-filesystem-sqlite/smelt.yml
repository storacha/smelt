version: 1
piri:
  nodes:
    - storage:
        db: sqlite # or 'postgres'
        blob: filesystem # or 's3'
    - storage:
        db: sqlite
        blob: filesystem
    - storage:
        db: sqlite
        blob: filesystem
